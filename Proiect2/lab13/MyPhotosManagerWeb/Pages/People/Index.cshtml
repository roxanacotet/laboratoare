@page
@model MyPhotosManagerWeb.Pages.People.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DetailDTO[0].IdPhoto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DetailDTO[0].DetailKey)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DetailDTO[0].DetailValue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DetailDTO) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdPhoto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DetailKey)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DetailValue)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
